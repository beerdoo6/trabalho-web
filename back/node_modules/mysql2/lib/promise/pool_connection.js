'use strict';

const PromiseConnection = require('mysql2/lib/promise/connection.js');
const BasePoolConnection = require('mysql2/lib/base/pool_connection.js');

class PromisePoolConnection extends PromiseConnection {
  constructor(connection, promiseImpl) {
    super(connection, promiseImpl);
  }

  destroy() {
    return BasePoolConnection.prototype.destroy.apply(
      this.connection,
      arguments
    );
  }
}

module.exports = PromisePoolConnection;
