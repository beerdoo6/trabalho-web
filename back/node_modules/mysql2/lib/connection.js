'use strict';

const BaseConnection = require('mysql2/lib/base/connection.js');

class Connection extends BaseConnection {
  promise(promiseImpl) {
    const PromiseConnection = require('mysql2/lib/promise/connection.js');
    return new PromiseConnection(this, promiseImpl);
  }
}

module.exports = Connection;
