'use strict';

const BasePool = require('mysql2/lib/base/pool.js');

class Pool extends BasePool {
  promise(promiseImpl) {
    const PromisePool = require('mysql2/lib/promise/pool.js');
    return new PromisePool(this, promiseImpl);
  }
}

module.exports = Pool;
